import type { PokemonCardMetadata } from "../types";

// Verified errata or local corrections belong here and must cite an official source.
export const pokemonMetadataOverrides: Partial<Record<string, Partial<PokemonCardMetadata>>> = {};
