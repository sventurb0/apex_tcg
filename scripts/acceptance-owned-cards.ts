import { coverageSummary, loadCollection } from "./collection-common";
const document = loadCollection(); const summary = coverageSummary(document); const result = { products: document.diagnostics.distinctOwnedProducts, copies: document.diagnostics.physicalCopies, families: summary.gameplayFamilies, ready: summary.simulationReady, unknownPrograms: summary.unknownPrograms, choiceWarnings: summary.choiceWarnings }; console.log(JSON.stringify(result, null, 2)); if (result.products !== 153 || result.copies !== 343 || result.unknownPrograms || result.choiceWarnings) process.exitCode = 1;
