import { coverageSummary, loadCollection } from "./collection-common";
const document = loadCollection(); const summary = coverageSummary(document); const result = { products: document.diagnostics.distinctOwnedProducts, copies: document.diagnostics.physicalCopies, families: summary.gameplayFamilies, ready: summary.simulationReady, complete: summary.complete, generated: summary.generated, partial: summary.partial, unsupported: summary.unsupported, missingClauses: summary.missingClauses, unknownPrograms: summary.unknownPrograms, choiceWarnings: summary.choiceWarnings }; console.log(JSON.stringify(result, null, 2)); if (result.products !== 153 || result.copies !== 343 || result.partial || result.unsupported || result.missingClauses || result.unknownPrograms || result.choiceWarnings) process.exitCode = 1;
